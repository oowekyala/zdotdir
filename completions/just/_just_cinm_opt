_just_cinm_opt() {
    # Whatever direnv put on PATH for this directory -- the installed cinm-opt
    # of the environment, rather than a build tree guessed from $(pwd).
    export MLIR_OPT_COMP_CMD="${commands[cinm-opt]:-$(pwd)/build/bin/cinm-opt}"
    _mlir_opt
}
